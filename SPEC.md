# SPEC.md — diamond-fuji-finder

<!-- scaffold template v1.8.0 から展開(2026-08-13)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

ダイヤモンド富士(太陽が富士山頂に重なって見える現象)が「いつ・どこで見られるか」を、
観測地点ごとに天文計算で求めて一覧表示する完全静的サイト。外部 API に依存せず、
すべての候補日時をクライアントサイドの決定的計算で導出する。運用コストゼロ。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | **太陽位置計算**: 任意の UTC 時刻・緯度・経度に対し、太陽の方位角(真北 0°・時計回り)と見かけ高度(大気差補正込み)を NOAA Solar Position アルゴリズムで計算する。精度目標は N-05 | must |
| F-02 | **富士山ジオメトリ**: 観測地点から富士山剣ヶ峰(北緯 35.36066°・東経 138.72743°・標高 3776 m)への測地方位角・距離(Vincenty 逆解、WGS84)と、地球曲率・大気屈折(屈折係数 k=0.13)補正込みの山頂見かけ仰角を計算する | must |
| F-03 | **ダイヤモンド窓探索**: 地点 × 日付ごとに、太陽方位角が山頂方位角と一致する時刻を予算付き二分探索(1 日あたり反復 ≤ 64)で求め、その時刻の太陽見かけ高度と山頂見かけ仰角の差 Δalt により候補判定する。判定閾値: `perfect` は \|Δalt\| ≤ 0.15°、`good` は \|Δalt\| ≤ 0.35°。閾値超過は非候補 | must |
| F-04 | **観測地点データ**: 定番観測スポット(12 地点以上)を手動キュレーションのデータとして保持する。各地点は id(一意)・名称・都道府県・緯度・経度・標高(m)・解説を持つ。スキーマはテストで検証する(T-030) | must |
| F-05 | **UI**: 地点一覧に地点ごとの「次の候補日」を表示し、地点詳細で今後 400 日分の候補日リスト(日付・時刻(JST)・Δalt・perfect/good)を表示する。基準日はクライアントで一度だけ取得して core へ引数として注入する | must |
| F-06 | **朝夕の自動判定**: 山頂方位角が 180° 未満(地点が富士山の西側)なら日の出ダイヤ、180° 以上(東側)なら日没ダイヤとして探索する。時刻表示はすべて JST(UTC+9 固定・DST なし) | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 完全静的(`next build` の静的エクスポート)。ランタイム依存は react / react-dom / next のみ | build 成功 + package.json の dependencies 検査 |
| N-02 | `src/core` は純関数のみ。カバレッジ lines/functions/statements ≥ 90%、branches ≥ 85% | `vitest run --coverage` の閾値 |
| N-03 | 決定性: 同一入力 → 同一出力。core で `Date.now()` / `Math.random()` / `new Date()`(引数なし)を呼ばない | T-006 / T-025 + eslint の import 制約 |
| N-04 | すべての探索は予算付き。方位一致時刻の探索は 1 日あたり反復 ≤ 64 で必ず停止する | T-020 |
| N-05 | 精度: NOAA 公表値に対し高度 ±0.2°・方位 ±0.5°・日出没時刻 ±3 分 | T-003〜T-005(既知値アンカー) |

## 4. 品質基準

数値基準(テストにトレースする):

- 太陽赤緯: 春分 \|δ\| < 0.5°、夏至 δ = +23.44° ± 0.15°、冬至 δ = −23.44° ± 0.15°(T-001/T-002)
- 東京(35.681°N, 139.767°E)の冬至日没時刻 16:32 JST ± 3 分(T-003)
- Vincenty 逆解: 赤道上の経度差 1° = 111.32 km ± 0.1%・方位角 90° ± 0.05°(T-010)
- 既知シーズンアンカー(実観測で知られる季節に候補日が出ること):
  - 高尾山山頂 → 12/10〜1/5 に日没候補 ≥ 1 日(T-021)
  - 田貫湖 → 4/10〜5/1 と 8/10〜8/31 に日の出候補 ≥ 1 日ずつ(T-023)
  - 稲村ヶ崎 → 3/25〜4/20 と 8/25〜9/20 に日没候補 ≥ 1 日ずつ(T-024)
- `npm run verify` green + `python harness/looplog.py validate` 合格が完了条件

## 5. スコープ外

- 天気予報連携(晴天確率の重ね合わせ)— v2 候補。core の入出力はそれを妨げない設計とする
- 地図表示・任意地点の入力(座標フォーム)・地点検索
- パール富士(月が山頂に重なる現象)
- 中間地形による視線遮蔽の判定(DEM 解析)。定番スポットのみ掲載することで回避する
- 過去日の検索(基準日より前)
